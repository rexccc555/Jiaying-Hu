# 仓库切换说明

本仓库（GitHub: `rexccc555/Jiaying-Hu`，原 takeadayoff）当前主内容已切换为 **CutPost 成片发布**。

## 本地归档（未推送到 GitHub）

原 takeadayoff 源码与本地依赖仍在本机目录 `_archive_takeadayoff/`（已在 `.gitignore` 中，不会提交）。需要时可从该目录恢复。

## 启动 CutPost

双击 `start.bat`，或见 `README.md` / `试用说明.md`。
